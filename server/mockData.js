// Mock data storage for all resources

// Mock users storage (shared between auth middleware and routes)
const mockUsers = {};

// Mock rooms storage
const mockRooms = {};

// Mock holdings storage
const mockHoldings = {};

module.exports = {
  mockUsers,
  mockRooms,
  mockHoldings
};
